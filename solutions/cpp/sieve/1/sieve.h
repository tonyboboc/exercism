#if !defined(SIEVE_H)
#define SIEVE_H
#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
namespace sieve {
std::vector<int> primes (int x);
}  // namespace sieve

#endif // SIEVE_H